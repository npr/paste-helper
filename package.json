{
  "name": "paste-helper",
  "version": "1.0.0",
  "description": "Simple paste helper for different browsers",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Max Doumit",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.2",
    "reload": "^2.2.2",
    "morgan": "^1.8.2",
    "supervisor": "^0.12.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "./node_modules/.bin/supervisor -k -e html -i .git/,node_modules/ -- app.js"
  }
}
